# plugin.json 正则匹配格式说明

> 本文档记录 `plugin.json` 中 `timestamp-regex` 特征所支持匹配的日期/时间戳格式，防止遗忘。uTools 中选中文本命中正则后即可唤起插件（`payload` 为该文本）。

## 一、纯数字时间戳（按位数自动切换精度）

| 正则 | 位数 | 精度 | 示例 |
| ---- | ---- | ---- | ---- |
| `/^\d{10}$/` | 10 位 | 秒 | `2026090708` |
| `/^\d{13}$/` | 13 位 | 毫秒 | `202609070823456` |
| `/^\d{16}$/` | 16 位 | 微秒 | `202609070823456789` |
| `/^\d{19}$/` | 19 位 | 纳秒 | `202609070823456789012` |

## 二、日期时间格式（两个正则覆盖）

### 1. 日期格式识别 `/^\d{4}[-/.]?\d{1,2}[-/.]?\d{1,2}$/`

| 匹配格式 | 示例 |
| -------- | ---- |
| `YYYY-MM-DD` | `2026-09-07` |
| `YYYY/MM/DD` | `2026/09/07` |
| `YYYY.MM.DD` | `2026.09.07` |
| `YYYYMMDD`（紧凑） | `20260907` |
| 单数字月/日 | `2026-9-7` |

### 2. 日期时间格式识别 `/^\d{4}[-/]\d{1,2}[-/]\d{1,2}[T ]\d{1,2}:\d{1,2}(:\d{1,2}(\.\d{1,9})?)?(Z|[+-]\d{2}:?\d{2})?$/`

分隔符 `[-/]` 均兼容，时间与日期之间支持 `T` 或空格，秒、小数（毫秒/微秒/纳秒）、时区依次可选：

| 匹配格式 | 示例 |
| -------- | ---- |
| `YYYY-MM-DD hh:mm` | `2026-09-07 14:23` |
| `YYYY-MM-DDThh:mm` | `2026-09-07T14:23` |
| `YYYY-MM-DD hh:mm:ss` | `2026-09-07 14:23:45` |
| `YYYY-MM-DDThh:mm:ss` | `2026-09-07T14:23:45` |
| 毫秒级 `.sss` | `2026-09-07T14:23:45.123` |
| 微秒级 `.ssssss` | `2026-09-07T14:23:45.123456` |
| 纳秒级 `.sssssssss` | `2026-09-07T14:23:45.123456789` |
| UTC 时区 `Z` | `2026-09-07T14:23:45.123Z` |
| 时区偏移 `+hh:00` | `2026-09-07T14:23:45.123+08:00` |
| 时区偏移 `+hh00` | `2026-09-07T14:23:45.123+0800` |
| 斜杠分隔 | `2026/09/07 14:23:45` |

示例最长约 35 字符：`2026-09-07T14:23:45.123456789+08:00`。

## 三、唤起后的填充行为

- **10 / 13 / 16 / 19 位数字**：直接定位到对应精度 Tab（秒/毫秒/微秒/纳秒），填入时间戳输入框。
- **日期字符串**：解析结果**始终填充到「日期 → 时间戳」的日期输入框**（年月日、时分秒，毫秒/微秒/纳秒按当前 TAB 精度显示）。主页精度 TAB 页的停留位置由系统设置里的「按日期字符串精度自动选择」开关控制（默认关闭）：
  - **开启**：主页随字符串小数位自动切换精度页——`.123`(3位)→毫秒、`.123456`(6位)→微秒、`.123456789`(9位)→纳秒、无小数→秒，保证小数位完整展开。
  - **关闭（默认）**：停在「默认进入 TAB 页」设置的精度页，小数位按该 TAB 精度显示（如秒级则不显示小数）。
- 小数部分支持到 **9 位（纳秒）**：`.123456789` 会拆分出毫秒 `123`、微秒 `456`、纳秒 `789`；「开启」模式下切换对应精度页后可在小数输入框完整显示，不会丢失。
- **时区联动**：字符串带 `Z` / `+0800` / `+08:00` 等时区信息时，自动将「输入时区」切换到该时区（`FIXED:+hhmm` 或 `UTC`），再据此换算时间戳。
- 解析失败（超范围等）时不填充日期输入框，回退为正常初始化。