# 计划：日期格式解析增强

> **状态更新（2026-09-09）**：本方案已实现落地（设置弹窗「日期解析」页、启用/自定义规则、美式/欧式歧义切换、剪贴板与 uTools 自动识别均已上线）。下方为原始设计记录；「现状结论」与行号对应当时代码，其中「识别成功但填不回」Bug 已随实现修复，`initTimestampInput` 剪贴板也已支持日期字符串识别与按位数切换精度 Tab。

## 目标

让「日期 → 时间戳」输入框支持更多日期书写格式：输入后即时解析，通过建议下拉选择后填回输入框；解析格式可通过配置开关控制（提升性能、消除歧义）；在 uTools 中粘贴/唤起时能自动识别这些日期字符串并填入。

覆盖格式示例：`2026-09-07`、`2026-09-07T14:23:45`、`2026-09-07T14:23:45.123`、`2026-09-07T14:23:45+08:00`、`2026-09-07T14:23:45.123Z`、`20260907T142345Z`、`2026-09-07T14:23:45.123+0800`、`Mon, 07 Sep 2026 06:23:45 GMT`、`2026/09/07`、`2026/9/7`、`2026年09月07日`、`2026年9月7日 14:23:45`、`09-07 14:23:45`、`09/07/2026`（美式 MM/DD）与 `07/09/2026`（欧式 DD/MM）、RFC 2822 等。

## 现状结论（已排查）

- `parseDate()`（index.js:1053）末尾已有 `Date.parse()` 兜底，ISO（含 T/偏移/Z）、RFC 2822、`20260907T142345Z` 等多数已在“转换”层能解析。
- 但**建议下拉** `buildSuggestions()`（index.js:1173）只有手写正则，仅支持相对词 + `YYYY[-/年]MM[-/月]DD[ 时间]`。
- **填回函数** `applyFullDateStr()`（index.js:1117）只认 `YYYY-MM-DD HH:mm:ss`；uTools 入口 `onPluginEnter`（index.js:3187 附近）对日期 payload 调 `parseDate` 成功后在 `applyFullDateStr` 处匹配失败 → **识别成功但填不回（Bug）**。
- `initTimestampInput()`（index.js:2207）剪贴板只识别纯数字时间戳，日期字符串不识别。

## 方案（5 步）

1. **解析引擎**
   - 用「启用格式列表」驱动，每项含：名称、规则（正则/模板）、字段语义（年月日时分秒毫秒、时区）、是否启用。
   - 优先级：显式带时区（ISO/RFC）→ 数字歧义格式 → `Date.parse` 兜底。
   - 返回统一结构 `{y,mo,d,h,mi,s,ms, tz?, srcFmt}`。

2. **建议下拉接入**
   - 输入任意启用格式 → 下拉显示解析结果建议（含来源格式名）。
   - 选中后填回 date/time/ms 输入框，复用现有建议/日历流程。

3. **填回重构**
   - `applyFullDateStr` 升级为通用填回：解析结果（含时区、毫秒）写回输入框。
   - 修复 uTools payload 填不回 Bug。

4. **格式配置**
   - 设置弹窗新增「日期解析」标签页（与「时区配置」「日期格式」并列）。
   - 每条格式可开关；**歧义风格单选**（美式 MM/DD 或欧式 DD/MM），持久化 localStorage。
   - 解析仅遍历启用格式以控制性能。

5. **uTools 识别**
   - 无关键词进入：剪贴板纯数字 ts → 时间戳框（沿用）；日期字符串 → 日期框并弹出建议。
   - 关键词为日期字符串 → 填回（含时区联动）。

## 已确认决策（2026-09-07）

1. 启动时机：本次只记录方案，之后单独启动实现。
2. 月/日歧义（`07/09/2026`、`09-07`）：按配置选一种解释（默认美式 MM/DD，可在配置中切换欧式 DD/MM）。
3. 配置入口：设置弹窗新增「日期解析」页。
4. 解析字符串带时区（`+08:00`/`Z`）：自动切换输入时区到该值，并按现有逻辑标记「自定义」（复用 `applyParsedTz`/标题联动）。
5. 剪贴板 / 无关键词 utools 进入：自动识别日期字符串填入日期框。

## 涉及现有代码

- `parseDate` / `tzFromDateString` / `buildSuggestions` / `showSuggestions` / `applyFullDateStr` / `readDateSelection` / `applyParsedTz`（index.js）
- 建议下拉点击处理 `dateSuggestEl`（index.js:2979）
- uTools 入口 `onPluginEnter` 与 `initTimestampInput`（index.js:3186 / index.js:2207）
- 设置弹窗结构（index.html `.modal`、`#pane-tz` / `#pane-fmt`，新增 `#pane-date-parse`）与 `initCsb` 滚动条注入
- i18n（I18N.zh / I18N.en，index.js:354-430）